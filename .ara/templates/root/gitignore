# Access data never goes into version control.
.env
.env.*

# A clone of an embedded place inside this root is a reference, not content of
# the root. Enter its folder here, one line per place:

.DS_Store
node_modules/
